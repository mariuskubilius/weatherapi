
var wwo = function(config) {
  var config = config || {};
  var config.format = config.format || 'json';
  var config.accountType = config.accountType || 'free';
  var url = config.url || 'http://api.worldweatheronline.com/';
}

var wwo.weather.prototype = function (location, options) {
  
}

var wwo.search.prototype = function (location, options) {
  
}
